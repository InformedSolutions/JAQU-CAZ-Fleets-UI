= link_to 'Back', matrix_payments_path, class: 'govuk-back-link'

%main.govuk-main-wrapper#main-content{role: 'main'}
  .govuk-grid-row
    .govuk-grid-column-two-thirds
      %h1.govuk-heading-l
        Confirm your payment

      = form_for('payment', url: initiate_payment_payments_path, method: :post, html: { id: 'payment-form' }) do |f|
        .govuk-form-group
          %dl.govuk-summary-list
            .govuk-summary-list__row
              %dt.govuk-summary-list__key
                Clean air zone
              %dd.govuk-summary-list__value
                = @zone.name
              %dd.govuk-summary-list__actions
                = link_to payments_path, class: 'govuk-link' do
                  Change
                  %span.govuk-visually-hidden
                    Clean Air Zone
            .govuk-summary-list__row
              %dt.govuk-summary-list__key
                Entries selected
              %dd.govuk-summary-list__value
                = @days_to_pay
              %dd.govuk-summary-list__actions
                = link_to matrix_payments_path, class: 'govuk-link' do
                  Change
                  %span.govuk-visually-hidden
                    dates
            .govuk-summary-list__row
              %dt.govuk-summary-list__key
                Total charge
              %dd.govuk-summary-list__value
                = "£#{@total_to_pay.to_i}"
              %dd.govuk-summary-list__actions
                = link_to('View details', review_details_payments_path)
          = f.submit 'Continue', class: 'govuk-button'
